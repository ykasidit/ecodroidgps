* use bluez-gatt-server github project to host the ble location and navigation profile
  - start/maintain bluez-gatt-server with required chrcs process running
    - handle already connected and cannot ad case - how to close existing ble conn first
  - parse nmea for lat lon etc
  - populate bit str for that position
  - mqtt pulish it to topic configured with

* install mosquitto on pi

* make git repo for binary so customer easy to update

* test on pi

* test with supported phones

* reply harald

* log all nmea to .txt.gz - if param provided