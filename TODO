- ansible script to
  - debain again
    - ansible install miniconda
      - conda install from url pandas numpy armhf
  - install dep
  - run bluez-gatt-server unittests
  - install edg from git@gitlab.com:ykasidit/ecodroidgps_bin.gitrun edg unittest and get running
- autostart
- do https://github.com/nils-werner/raspi-overlayroot
  - do not change/use fstab
  - for ro mount - edit /etc/boot.txt then run sudo ./mkscr
- test well
- reduce debug print level

- alarm not seeing usb through hub
- autostart systemd: cd /home/alarm/edg && bash start.sh/

- gatt device name

- take custom name from /config/custom_name  

python test_parse_gps.py
- rm debug prints
- mqtt pub through api?
- test on real dev

* log all nmea to .txt.gz - if param provided